This directory contains the `general` task data directory.

The compressed multimodal data is stored in `coin/`. Before using this task, extract the archive inside `coin/` into the current `coin/` directory.
