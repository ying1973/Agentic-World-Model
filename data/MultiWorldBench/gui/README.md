This directory contains the `gui` task data directory.

The compressed multimodal data is stored in `mobileworld/`. Before using this task, extract the archive inside `mobileworld/` into the current `mobileworld/` directory.
