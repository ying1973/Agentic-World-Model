This directory contains the `embodied` task data directory.

The compressed multimodal data is stored in `droid/`. Before using this task, extract the archive inside `droid/` into the current `droid/` directory.
