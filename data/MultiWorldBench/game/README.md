This directory contains the `game` task data directory.

The compressed multimodal data is stored in `atari/`. Before using this task, extract the archive inside `atari/` into the current `atari/` directory.
